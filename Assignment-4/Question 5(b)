#include<stdio.h>
#define SIZE 5

int queue[SIZE];
int front = -1, rear = -1;

void enqueue(int value) {
    if(rear == SIZE-1) {
        printf("Queue full!\n");
    } else {
        if(front == -1) front = 0;
        rear++;
        queue[rear] = value;
    }
}

int dequeue() {
    if(front == -1) return -1;
    int value = queue[front];
    front++;
    if(front > rear) front = rear = -1;
    return value;
}

void push(int value) {
    enqueue(value);
    printf("Pushed: %d\n", value);
}

void pop() {
    if(front == -1) {
        printf("Stack empty!\n");
        return;
    }
    
    // Rotate queue to bring last element to front
    int size = 0;
    for(int i=front; i<=rear; i++) size++;
    
    for(int i=0; i<size-1; i++) {
        int val = dequeue();
        enqueue(val);
    }
    
    int popped = dequeue();
    printf("Popped: %d\n", popped);
}

int main() {
    push(5);
    push(15);
    push(25);
    pop();
    pop();
    
    return 0;
}
